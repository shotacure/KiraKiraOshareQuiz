{
  "name": "KiraKiraOshareQuiz",
  "version": "1.0.2",
  "private": true,
  "description": "Real-time quiz event management app (backend)",
  "type": "module",
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy",
    "deploy:guided": "sam deploy --guided",
    "delete": "sam delete --stack-name quiz-app",
    "load-quizzes": "node scripts/load-quizzes.mjs"
  },
  "devDependencies": {
    "ws": "^8.17.0"
  }
}
